.DS_Store
__pycache__/
*.py[cod]
*.3dmbak
*.gh.bak
*.autosave

# The componentizer's downloaded GH_IO.dll
temp/

# dist/ghuser holds the prebuilt User Objects and is committed on purpose so
# they can be downloaded without a GitHub account. Everything else under
# dist/ is build scratch.
dist/*
!dist/ghuser/

# Claude Code local state (worktrees and machine-local settings)
.claude/worktrees/
.claude/settings.local.json
